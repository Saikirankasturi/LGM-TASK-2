import React from 'react';
import ReactDOM from 'react-dom';
import App from '../src/components/App';

ReactDOM.render(
  <React.StrictMode>
    <App />
    <strong> Hi Saikiran,</strong> 
  </React.StrictMode>,
  document.getElementById('root')
);
